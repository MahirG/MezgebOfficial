# Subscription interaction regression

- Plan actions must remain clickable on touch and pointer devices.
- Signed-out users return to the selected plan after authentication.
- The current plan action opens account subscription management instead of rendering as a disabled control.
- Decorative card layers must not capture pointer events.
